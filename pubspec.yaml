name: google_maps_cluster_manager
version: 3.1.0
description: Local vendored minimal shim of google_maps_cluster_manager for this project
environment:
  sdk: ">=3.0.0 <4.0.0"
flutter:
  plugin:
    platforms: {}

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.10.1
